<div class="container">
  <div class="row">
    <% flash.each do |type, message| %>
	  <div id= "flash-messages" class="alert <%= bootstrap_class_for(type) %> fade in">
		<button class="close" data-dismiss="alert">&times;</button>
		<%= message %>  
	  </div>
	<% end %>
  </div>
</div>