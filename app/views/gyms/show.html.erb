<p id="notice"><%= notice %></p>

<h3>There are <%= @gym.routes.count %> active climbs at <%= @gym.name %></h3>

<div id="chartz" style="width:100%; height:400px;"></div>

<div class="container">
  <div class="wrapper">
    <div class="row">
      <div class="col-md-12">
        <h2><%= @gym.name %></h2>
        <p>Location: <%= @gym.location %></p>
        <%= link_to 'Edit gym info', edit_gym_path(@gym) %>
      </div>
    </div>

    <hr>

    <div class="row">
      <% @walls.each do |wall| %>
        <div class="col-md-4">
          <div class="thumbnail">
          	<image src="#">
            <div class="caption">
              <h4><%= link_to wall.name.capitalize, wall %></h4>
              <p><%= wall.routes.count %></p>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
  <%= link_to 'Back', gyms_path %>
</div>

<%= link_to "Create a New Wall", new_gym_wall_path(@gym) %>


<%= high_chart("chartz", @chart) %>
